# ===========================================
# Configuración de Entorno - Eventos Viña
# ===========================================

# SendGrid Configuration (REQUERIDO para emails)
SENDGRID_API_KEY=your-sendgrid-api-key
SENDGRID_FROM=mail@example.com

# Host IP (para acceso desde red local)
# Dejar vacío para localhost
# Para red local, usar IP del host (ej: 192.168.1.100)
# Obtén tu IP con: ipconfig (Windows) o ip addr (Linux)
HOST_IP=

# Node.js Server Configuration
PORT=4000

# Flask API Configuration
FLASK_PORT=5001
FLASK_DEBUG=False
SECRET_KEY=your-secret-key-change-in-production

# Database Configuration
# Para SQLite (desarrollo local y Docker)
DATABASE_URL=sqlite:///entradas.db
# Para PostgreSQL (producción - futuro)
# DATABASE_URL=postgresql://username:password@localhost:5432/entradas_db

# ===========================================
# INSTRUCCIONES:
# ===========================================
# 1. Copia este archivo como .env:
#    cp .env.example .env
#
# 2. Configura SendGrid (requerido para emails):
#    a) Regístrate en https://sendgrid.com (gratis: 100 emails/día)
#    b) Settings > API Keys > Create API Key (Full Access)
#    c) Copia la clave a SENDGRID_API_KEY
#    d) Settings > Sender Authentication > Verify email
#    e) Usa ese email en SENDGRID_FROM
#
# 3. Para localhost (mismo computador):
#    HOST_IP=
#    Ejecuta: docker-compose up -d
#    Accede: http://localhost:3000
#
# 4. Para red local (otro computador):
#    a) Obtén tu IP: ipconfig (Windows)
#    b) HOST_IP=192.168.1.100 (tu IP real)
#    c) Ejecuta: docker-compose up -d
#    d) Configura firewall (ver DESPLIEGUE_REMOTO.md)
#    e) Accede: http://192.168.1.100:3000
#
# 5. Scripts automáticos disponibles:
#    - Windows PowerShell: .\configure-network.ps1
#    - Windows CMD: run-network.bat
#    - Desarrollo local: .\docker-build-and-run.ps1
# ===========================================
